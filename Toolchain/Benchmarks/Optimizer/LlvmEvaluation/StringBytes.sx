use Interop.C

func main() {
    let text = "é\0A"
    print(C.byte_count(text))
    print(C.byte_at(text, 0 as uint))
    print(C.byte_at(text, 1 as uint))
    print(C.byte_at(text, 2 as uint))
    print(C.byte_at(text, 3 as uint))
}
