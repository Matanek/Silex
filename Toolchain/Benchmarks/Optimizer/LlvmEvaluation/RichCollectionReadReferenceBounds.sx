class Item {
    var value:int
    init(value:int) { self.value = value }
}

func read(values:Item[], index:int) int {
    let item:@Item = @values[index]
    return item.value
}

func main() {
    var values:Item[] = [Item(10), Item(20)]
    print(7)
    print(read(values, 2))
    print(8)
}
