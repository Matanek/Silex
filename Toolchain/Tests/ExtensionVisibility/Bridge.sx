use ExtensionVisibility.Core as Core
use ExtensionVisibility.Extras as Extras
use Core.Counter

public func advanced(value:int) int {
    var counter = Counter(value:value)
    return counter.bump()
}
