use Math

extend Math.Vec2 {
    func scaled() float {
        return self.length_squared()
    }

    pub static func origin() Math.Vec2 {
        return Math.Vec2()
    }
}

extend Math.Canvas {
    func hidden() {
    }
}
